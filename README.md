# SUNPATHS browser playtest — repaired build

This static GitHub Pages build requires no npm installation.

## Repairs in this build

- Fixed the blank screen after the human player's second Development action.
- Added a visible crash screen instead of a white page if another UI error occurs.
- New Game now generates a different random seed automatically.
- The same typed seed still reproduces the same weather, Local Conditions, Innovation Market and AI tie-breaks.
- Added a **Generate new seed** button on setup.
- Weather, Local Conditions, resources, installed technologies, build options, energy stages, Knowledge, and Light windows now open explanation cards when clicked.

## Upload

Upload the contents of this folder to the root of your GitHub repository. Replace the old `app` folder and `index.html`, then commit. GitHub Pages will update automatically.
