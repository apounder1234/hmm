SUNPATHS Candidate A.5.1 — Browser-fixed static package

GitHub Pages deployment:
1. Upload every file and folder in this package to the repository root.
2. Keep the app/ directory structure unchanged.
3. Enable GitHub Pages for the branch/root directory.
4. Open index.html through the GitHub Pages URL, not GitHub's source-file preview.

This build contains its React interface runtime locally. It does not require
esm.sh, unpkg, jsDelivr, npm, or a build command.
